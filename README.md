Build a virtual environment by using the terminal and type  
  cd bash
  python3 -m venv venv
  source venv/bin/activate
After activation:
  install packages: pip install requests
  install requirements.txt: pip install -r requirements.txt
How to run the application: python3 Felix_Nancy_Felix_1_repo_052026.py
  
